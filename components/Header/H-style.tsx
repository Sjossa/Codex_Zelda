import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  Header: {
    backgroundColor: "red",
    position: "fixed",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
  },
});
