import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  Header: {
    backgroundColor: "transparent",
    position: "absolute",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
  },
});
